name: Repository health

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  repository-health:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Verify Git tree
        run: git diff --check
